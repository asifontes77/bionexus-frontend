import fs from "node:fs";import path from "node:path";const root=process.cwd(),read=p=>fs.readFileSync(path.join(root,p),"utf8");const component=read("src/components/regional/BioNexusRegionalDateInput.vue"),view=read("src/views/PatientResultsEmailView.vue");for(const value of ["formatRegionalFunctionalDate","useRegionalSettingsStore","showPicker","update:modelValue","yyyy-MM-dd","dd-MM-yyyy","DD/MM/AAAA",":min=\"dateFrom\""])if(!(component+view).includes(value))throw new Error("REGIONAL_DATE_CONTRACT_MISSING:"+value);if(view.includes('type="date"'))throw new Error("NATIVE_DATE_INPUT_REMAINS_IN_VIEW");const uses=(view.match(/BioNexusRegionalDateInput/g)||[]).length;if(uses<3)throw new Error("REGIONAL_DATE_COMPONENT_USAGE_MISSING");console.log("[OK] Contrato del control de fecha regional aprobado.");
