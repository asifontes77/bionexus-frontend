import fs from "node:fs";import path from "node:path";const root=process.cwd(),read=p=>fs.readFileSync(path.join(root,p),"utf8");const view=read("src/views/TaxesView.vue"),dialog=read("src/components/tax/TaxDialog.vue"),formatter=read("src/services/regionalFormatter.js");for(const token of ["formatRegionalNumber","useRegionalSettingsStore","minimumFractionDigits: regionalSettings.settings.monetary_decimals","maximumFractionDigits: regionalSettings.settings.monetary_decimals"])if(!view.includes(token))throw new Error("TAX_VIEW_REGIONAL_MISSING:"+token);if(view.includes("Number(value).toFixed(2)"))throw new Error("TAX_VIEW_FIXED_FORMAT_REMAINS");for(const token of ["parseRegionalNumber","inputmode=\"decimal\"","percentagePlaceholder","value: numericValue"])if(!dialog.includes(token))throw new Error("TAX_DIALOG_REGIONAL_MISSING:"+token);for(const token of ["export function parseRegionalNumber","decimal_separator"])if(!formatter.includes(token))throw new Error("REGIONAL_PARSER_MISSING:"+token);console.log("[OK] Contrato regional de Impuestos aprobado.");
