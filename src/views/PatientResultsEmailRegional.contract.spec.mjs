import fs from "node:fs";import path from "node:path";const root=process.cwd(),read=p=>fs.readFileSync(path.join(root,p),"utf8");const main=read("src/views/PatientResultsEmailView.vue"),history=read("src/components/patients/PatientResultsEmailHistoryDialog.vue");for(const value of ['formatRegionalDateTime } from "@/services/regionalFormatter"','useRegionalSettingsStore } from "@/stores/regionalSettings"','const regionalSettings = useRegionalSettingsStore();','formatRegionalDateTime(`${date}T${time}`, regionalSettings.settings)'])if(!main.includes(value))throw new Error("MAIN_REGIONAL_MISSING:"+value);for(const value of ['const suffix = hour >= 12','${match[3]}-${match[2]}-${match[1]}'])if(main.includes(value))throw new Error("LEGACY_FORMAT_REMAINS:"+value);if(history.includes(':export-options="false"')||!history.includes('historial-envios-resultados'))throw new Error("HISTORY_EXPORT_INVALID");console.log("[OK] Entrega de resultados regional aprobada.");
