const t=v=>typeof v==='string'?v.trim():'';export const normalizeCurrency=(v={})=>({id:Number(v.id)||0,code:t(v.code).toUpperCase(),name:t(v.name),symbol:t(v.symbol),symbolPosition:v.symbolPosition??v.symbol_position??'before',decimalPlaces:Number(v.decimalPlaces??v.decimal_places??2),isLocal:Boolean(v.isLocal??v.is_local),isBase:Boolean(v.isBase??v.is_base),isActive:v.isActive??v.is_active??true,displayOrder:Number(v.displayOrder??v.display_order??0)});export const normalizeCurrencies=v=>(Array.isArray(v)?v:[]).map(normalizeCurrency);export const currencyPayload=v=>({code:t(v.code).toUpperCase(),name:t(v.name),symbol:t(v.symbol),symbolPosition:v.symbolPosition,decimalPlaces:Number(v.decimalPlaces)});
